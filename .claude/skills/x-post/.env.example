# Copy to .env (or run scripts/setup.sh which writes ~/.config/typefully/config.json)
TYPEFULLY_API_KEY=tk_xxx
TYPEFULLY_SOCIAL_SET_ID=ss_xxx
# Optional: override base for testing
# TYPEFULLY_API_BASE=https://api.typefully.com/v2
