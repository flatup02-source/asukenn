# CLAUDE.md — プロジェクトの記憶と運用ルール

このリポジトリで Claude Code セッションを開始したら、まず本ファイルを読むこと。
セッション間で残るのは **gitにコミットされたファイルだけ**（ephemeralコンテナ）。

## 1. プロジェクト概要
- `asukenn` は Firebase / Netlify / Dify を組み合わせた個人プロジェクト
- 直近の主要追加: **X運用自動化スキル** (`/x-post`, `/weekly-plan`) by branch `claude/typefully-x-automation-FTcI2`
- 詳細: `.claude/skills/x-post/README.md`

## 2. 永続記憶の仕組み（このリポでの実装）

「Agentmemory サーバー (port 3111)」のような外部メモリは **このコンテナでは動かない**。
よって永続記憶はすべて **git にコミットされる Markdown ファイル** で管理する。

| 種類 | 置き場所 | 内容 |
|---|---|---|
| 不変ルール・憲法 | `CLAUDE.md`（本ファイル） | 全セッションで読まれる前提のもの |
| 設計判断ログ | `.claude/memory/decisions.md` | なぜその選択をしたか・トレードオフ |
| 再利用パターン | `.claude/memory/patterns.md` | コードパターン・プロンプトパターン |
| 経験的学習 | `.claude/memory/lessons.md` | バグ修正・運用での気づき |
| スキル定義 | `.claude/skills/<name>/SKILL.md` | 動的に呼ばれるワークフロー |
| スラッシュコマンド | `.claude/commands/<name>.md` | `/<name>` の入口 |
| 自動化 | `.claude/settings.json` の `hooks` | 「常に〜する」系の挙動 |

将来 Agentmemory MCP を本当に導入する場合は、`.mcp.json` に追加し、本ファイルの
「2.」セクションを書き換えること。それまでは **git first** で運用する。

## 3. 運用憲法（Hybrid Human-in-the-Loop）
1. **公開は人間の明示承認後のみ**。Claude は `--immediate` を勝手に付けない。
2. **1日のX投稿は最大5本**。超えそうなら翌日以降に分散提案する。
3. **オリジナル比率 ≥ 70%**。翻訳・引用解説は週全体の30%まで。
4. **APIキーは絶対にコミットしない**（`.gitignore` に集約済）。設定ファイルは chmod 600。
5. **自動リプライ・自動いいね・自動フォローは禁止**。
6. 矛盾を見つけたら **即座に本ファイルを更新する PR を出す**（黙って従わない）。

## 4. セッション開始時の挙動
1. 本ファイルと `.claude/memory/*.md` を読む（`Read` ツールで）。
2. ユーザーのタスクに関連する `decisions.md` / `lessons.md` を引用しながら答える。
3. 関連する記憶が無い場合はその旨を明記してから提案する（「過去の判断は見当たらないので新規提案します」）。

## 5. 知識の振り分けルール
新しい知識が発生したとき、以下の判定で書き込む先を決める:
- **常に守るべきルール？** → 本ファイルに追記
- **その時の判断とトレードオフ？** → `decisions.md` に append（日付＋見出し）
- **再利用しそうな雛形？** → `patterns.md`
- **失敗から学んだこと？** → `lessons.md`
- **スキル/コマンドとして動くべき？** → `.claude/skills/` か `.claude/commands/` を作る/更新

## 6. 矛盾解消
- 過去の記憶と新しい指示が食い違う場合、**ユーザーに確認してから記憶を更新**する。
- 「黙って上書き」「黙って従う」のどちらもしない。
- 更新時は対象ファイルの該当行に日付付きで `(updated 2026-MM-DD: <理由>)` と注記。

## 7. 関連スキル
- `/x-post <URL>` — 参考URLから日本語Xスレッドを構成し Typefully に下書き保存
- `/weekly-plan monday|review|friday|sunday|daily|status` — 週間運用フロー
- どちらも **デフォルトは下書きどまり**

## 8. このファイルを更新する条件
- 運用憲法（3章）に追加・例外が発生したとき
- スキル一覧（7章）に新規追加・廃止があるとき
- 記憶の置き場所構造（2章）が変わったとき
それ以外（個別判断・学び）は `.claude/memory/*.md` 側に書く。
